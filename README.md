# Travis CI

## 操作代码
```
# 列出所有的分支
git branch -a

# 克隆代码
git clone -b init https://github.com/ztj1993/travis-ci.git ./init

# 拉取代码
git -C ./init pull

# 提交代码
git -C ./init add .
git -C ./init commit -m "init"

# 推送代码
git -C ./init push
```
